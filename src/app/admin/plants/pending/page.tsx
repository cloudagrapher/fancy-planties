import 'server-only';

export default async function AdminPendingPlants() {
  return (
    <div className="admin-pending-plants">
      <h1>Pending Plant Approvals</h1>
      <p>Plant approval workflow will be implemented in a future task.</p>
    </div>
  );
}